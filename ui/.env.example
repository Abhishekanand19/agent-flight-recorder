# Set on Vercel to your Railway backend URL (no trailing slash).
# Leave UNSET for local development — calls stay relative and use the Vite
# proxy to localhost:8000 (see vite.config.js).
VITE_API_URL=https://your-backend.up.railway.app
